abstract class BaseQuizeRepository{
    Future<List<Question>> getQuestions({
        int numQuestions,  //..now many ques the API will return 
        int categoryId,
        Difficulty difficulty,
    })
  
    }
